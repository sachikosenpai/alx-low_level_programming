#include "main.h"

/**
 * _puts_recursion - function that prints a string along with a new line
 * @s: character
 *
 */

void _puts_recursion(char *s)
{
	_puts_recursion(%s, char*s);

		_putchar('\n');
}
