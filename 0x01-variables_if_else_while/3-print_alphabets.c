#include <stdio.h>
/**
 * main - print alphabet in lower case then in upper case
 *
 * Return: Always (0)
 */
int main(void)
{
	char c = 'a';
	char d = 'A';

	while (c <= 'z' )
	{
	putchar(C);
	c++;
}
	while (d <= 'Z' )
{
	putchar(d);
	d++;
}
	putchar('\n');
	return (0);
