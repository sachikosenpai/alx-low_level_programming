#include "main.h"

/**
 * main print alphabet in lower case
 *
 * Return - Always(0)
 */

void print_alphabet(void)
{
	char alphabet
		for ( alphabet = 'a'; letter<= 'z'; alphabet++)
			_putchar(letter);

	putchar('\n');
}
