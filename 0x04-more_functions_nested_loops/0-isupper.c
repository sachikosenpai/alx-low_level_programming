#include "main.h"

/**
 * _isupper - checks for upper case chracter
 * @c: character to be checked
 *
 * Return: 1 if c is uppercase otherwise 0
 */

int _isupper(int c)
{
	char c;

	if (c >= 'A' && c<= 'Z')

	return (1);


	else
		return (0);
}
